#include <stdio.h>

int foo() {
    printf("OK\n");
    return 42;
}

int add(int x, int y) {
    return x + y;
}

int add6(int a, int b, int c, int d, int e, int f) {
    return a + b + c + d + e + f;
}
